/* FILIMON ADRIAN, 334CC */
#include<stdio.h>

int 
func_multiple_lines (float arg1, float arg2)
{
    char multiple1, /* comment */ multiple2, multiple3;
    float multiple4, multiple5;

    return multiple1;
}

int
main() {

    /* skip this comment */
    // single line comment
    double n1 = 1, n2 = 2, n3 = 3;
    printf("Skip this function ");

    /* if n1 is greater than both n2 and n3, n1 is the largest */
    if (n1 >= n2 && n1 >= n3)
        printf("%.2lf is the largest number.\n", n1);

    else if (n2 >= n1 && n2 >= n3)
        printf("%.2lf is the largest number.\n", n2);

    else
        printf("%.2lf is the largest number.\n", n3);

    return 0;
}
